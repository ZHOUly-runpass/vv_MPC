# 开发机执行报告

日期：2026-08-25

## 环境

- 主机：`guanpc`
- Ubuntu 22.04.5、ROS 2 Humble、Python 3.10
- NVIDIA GeForce RTX 4090，24 GB 显存
- GitHub 克隆目录：`/home/zhou/E2Eproject_MPC/github_pull/05`

## 已确认

- GitHub 克隆、23 项测试、配置校验、合成闭环、CasADi 求解、colcon 编译和 ROS 零速实启均通过。
- ROS 零速实启时 `/cmd_vel` 所有分量为 0，状态为 `commissioning_lock`。
- 11 类故障注入均触发停止：命令/心跳/点云/里程计/IMU/TF 过期、求解超时、急停、硬件故障、障碍物紧急和非有限命令。
- 参数化问题缓存后，20 步 MPC 热态耗时：1 个近场障碍约 10 ms，8 个约 35 ms，均满足 80 ms；32 个近场障碍约 122 ms，会触发超时停车。
- 严格可达域筛选可安全剔除规划周期内物理上不可接触的远处障碍；32 个远处障碍筛选为 0 个活动约束后约 16.5 ms。
- RTX 4090 上 7 候选、10 控制间隔的 PyTorch 规划/安全头完成前向与反向，损失为有限值。
- ROS 适配器包含独立于点云回调的定时 watchdog；主循环完全停止时仍会因点云、odom、IMU、TF 或规划心跳过期而重复发布零速。

## ROS 图结论

当前 ROS 图属于机械臂与 MoveIt/Gazebo 任务。R680 所需 `/cmd_vel`、`/odom`、`/imu/data_raw`、`/point_cloud_raw`、`/plan` 和局部代价地图均未在线，因此不能进行 C16/R680 实测验收。

## checkpoint 结论

发现的 `SafetyConsistentPlanner` 权重 SHA-256 为 `828d1cc4dcd3a006f0812321ef6569e536adc3b19c58de76a73a29e20b79cb47`，大小约 4.3 MB。其结构是 ego/agent/map/route 编码器和 IL head，配置为 4 候选、8 个未来点；它不是 UniLION 或 C16 LiDAR backbone，不能用于关闭 `frozen_backbone_verified_on_c16` 验证门。

## 保持关闭的项目

- 小车驱动与 C16 当前未在线。
- 车型、轮廓、物理参数、TF 外参、控制方向、制动距离和急停尚无实测证据。
- UniLION 代码版本与纯 LiDAR checkpoint 未提供。
- 32 个同时位于近场可达域的最坏场景仍超过 80 ms，必须停车或后续切换稀疏求解器；完整 C16 感知链端到端延迟仍待真机数据。
