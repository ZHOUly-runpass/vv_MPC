# R680-C16 冻结 LiDAR 主干与 D-CBF-MPC 完整算法流程

> 文档状态：首版可实施设计  
> 适用平台：WHEELTEC R680 系列 + 镭神 C16 16线三维激光雷达  
> 配套配置：`configs/robot/r680_c16.yaml`  
> 安全状态：底盘类型、车体几何和实车限制确认前，只允许感知与日志采集，禁止车辆运动。

## 1. 目标与边界

算法目标是在 R680 小车上实现以下闭环：

```text
C16三维点云 + odom/IMU + Nav2路径
  -> 冻结LiDAR通用几何主干
  -> 小车域特征适配
  -> 运动学可行的多候选轨迹
  -> 风险预测与候选排序
  -> 车型专用D-CBF-MPC验证/修正
  -> 车载安全监督
  -> /cmd_vel
  -> C50C底盘
```

研究算法负责提高规划质量，确定性安全链负责在网络、求解器或感知异常时停车。学习模型的风险分数不能替代真实几何约束、D-CBF约束、命令超时保护和物理急停。

## 2. 模块冻结与训练边界

### 2.1 第一阶段固定冻结

- LiDAR voxel/pillar encoder；
- LiDAR backbone前部和中部；
- 大部分BEV backbone；
- 所有冻结模块的BatchNorm统计量；
- 第三方checkpoint对应的输入网格和通道约定。

### 2.2 条件冻结

- 最后一个BEV block；
- temporal encoder；
- 原检测、跟踪和运动预测头。

条件冻结模块只在C16点云验真通过后使用。若冻结特征存在明显域偏移，仅允许最后一个BEV/temporal block以head学习率的`0.01~0.05`倍解冻。

### 2.3 重新训练或替换

- `C16FeatureAdapter`；
- `RouteEncoder`与`CostmapEncoder`；
- `EgoEncoder`；
- 车型相关`MultiCandidateHead`；
- `SafetyPredictionHeads`；
- `CandidateRanker`；
- 本地障碍物跟踪与运动预测模块。

原nuScenes地图头不用于小车导航；原检测头只能提供可选语义信息，不能成为D-CBF的唯一障碍来源。

## 3. 系统进程与部署边界

```text
R680车载主控
  ├─ C16驱动
  ├─ turn_on_wheeltec_robot
  ├─ odom/IMU/EKF/TF
  ├─ 几何障碍物管线
  ├─ 快速安全监督与watchdog
  └─ /cmd_vel -> C50C

开发机或GPU主控
  ├─ UniLION/CenterPoint冻结主干
  ├─ feature adapter
  ├─ candidate/safety/ranker heads
  └─ D-CBF-MPC（达到实时性后可迁移到车载端）
```

无论学习规划器部署在哪一端，命令新鲜度检查、速度限幅、急停和失联清零必须在车载端运行。

## 4. 启动状态机

```text
BOOT
 -> SELF_TEST_GUARD
 -> SENSOR_CHECK
 -> FRAME_CHECK
 -> PERCEPTION_ONLY
 -> MOTION_ARMED
 -> AUTONOMOUS
 -> DEGRADED_STOP / EMERGENCY_STOP
```

### 4.1 `BOOT`

1. 检查C50C使能、急停、电池电压和主控健康状态。
2. 默认发布零速度，不启动自动规划。
3. 加载配置并验证所有必填字段。
4. `vehicle.variant`、footprint或控制限制未确认时，拒绝进入`MOTION_ARMED`。

### 4.2 `SELF_TEST_GUARD`

R680上电后可能等待约10秒并向前自检约15 cm。首次调试必须架空车轮或清空前方区域；确认固件行为前，算法不能假设上电期间底盘静止。

### 4.3 `SENSOR_CHECK`

检查：

- `/point_cloud_raw`类型为`PointCloud2`；
- 点云包含有限的`x/y/z`，并核对`intensity/ring/time`；
- `odom`和`/imu/data_raw`持续更新；
- 点云、里程计和IMU时间戳单调；
- 发布频率及延迟在配置阈值内。

### 4.4 `FRAME_CHECK`

验证TF链：

```text
map -> odom_combined -> base_footprint -> base_link -> laser
```

同时验证坐标约定为`x前、y左、z上、yaw逆时针为正`。正负方向必须通过低速实车试验确认，不能只依赖存在冲突的协议文字。

### 4.5 `PERCEPTION_ONLY`

允许点云、特征、障碍物和候选轨迹运行，但输出始终被安全监督替换为零速度。所有实车必填参数确认后，才允许人工解锁`MOTION_ARMED`。

## 5. 在线闭环流程

### 5.1 输入采集

每个规划周期读取：

```text
SensorPacket
  point_cloud: PointCloud2
  cloud_timestamp
  odometry
  imu
  tf_snapshot
  global_path
  local_costmap
  chassis_state
  emergency_stop_state
```

每份数据保留原始时间戳和接收时间戳，用于计算传感、网络、推理、求解与执行总延迟。

### 5.2 C16点云预处理

1. 校验字段、字节序和有限值。
2. 删除NaN/Inf、零点、超量程点和自车反射点。
3. 使用TF转换到`base_footprint`。
4. 利用odom/IMU进行扫描内或帧间运动补偿。
5. 裁剪到局部规划ROI。
6. 标准化intensity；保留ring和relative time。
7. 统计每帧点数、距离分桶密度、丢点率和遮挡率。

输出：

```text
LidarSequence
  points: list[[N, x,y,z,intensity,relative_time,ring]]
  timestamps
  ego_poses
  lidar_to_base
  quality_metrics
```

### 5.3 双通路感知

#### 学习特征通路

```text
LidarSequence
 -> UniLION temporal LiDAR（主研究路线）
    或 CenterPoint（工程fallback）
 -> FrozenSceneFeatures
```

UniLION只有通过纯LiDAR checkpoint、C16输入、连续100帧无NaN/Inf及实时性验收后才能设为默认。否则使用CenterPoint或单帧工程基线。

#### 确定性几何通路

```text
C16点云
 -> 地面/自车点过滤
 -> 欧式聚类或占据栅格
 -> 静态墙体与自由空间边界
 -> 局部目标跟踪
 -> 障碍物状态与协方差
```

该通路始终启用，即使冻结检测头没有识别障碍类别，也必须向D-CBF提供几何约束。

### 5.4 统一冻结特征契约

```text
FrozenSceneFeatures
  schema_version
  bev_feature: [B,C,H,W]
  agent_feature: [B,A,Ca]             # 可选
  agent_state: [B,A,7]                # x,y,yaw,vx,vy,length,width
  agent_future: [B,A,M,T,2]           # 可选
  agent_covariance: [B,A,T,2,2]
  valid_mask: [B,A]
  source_backend
  checkpoint_hash
  input_quality
```

第三方模型的私有字典不得直接进入规划头。

### 5.5 自车与路径编码

1. 将Nav2 global path变换到`base_footprint`。
2. 按弧长采样局部路线并计算航向、曲率和终点距离。
3. 从odom/IMU构造速度、角速度和短时历史。
4. 从local costmap提取可通行走廊和未知区域。
5. `C16FeatureAdapter`融合BEV、路线、自车历史、costmap和点云质量指标。

### 5.6 车型相关候选生成

底盘类型由实车`car_mode/OLED TYPE`唯一确定：

| 车型 | 状态 | 内部控制 | 必须覆盖的候选 |
|---|---|---|---|
| 差速/四驱滑移 | `[x,y,yaw,v,omega]` | `[a_v,a_omega]` | 直行、左右弧、原地转向、减速、停车 |
| 麦轮/全向 | `[x,y,yaw,vx,vy,omega]` | `[a_x,a_y,a_omega]` | 前进、横移、旋转对正、绕行、停车 |
| 阿克曼 | `[x,y,yaw,v]` | `[a,delta]` | 路径跟踪、左右曲率偏置、减速、停车 |

候选头优先输出控制结点，再通过对应运动学模型rollout，保证候选天然满足基本运动学。禁止阿克曼车生成横移或原地旋转候选。

### 5.7 解析预过滤

对每条候选计算：

- 是否覆盖完整规划时域；
- 速度、加速度、角速度/转角及变化率；
- 曲率和最小转弯半径；
- route偏离和costmap越界；
- 与几何障碍的最小距离；
- TTC、停车距离和未知区域占用；
- 点云或状态数据是否过期。

硬约束不合格的候选直接剔除，不送入ranker或MPC。

### 5.8 安全预测与排序

训练头输出：

```text
predicted_h_min
feasibility_probability
predicted_correction
predicted_risk
predicted_slack
rank_score
```

排序综合进度、路线偏差、舒适性、风险、预计MPC修正量和可行性。ranker只负责选择Top-K，不能替代在线D-CBF-MPC。

### 5.9 障碍物预测与不确定度

每个障碍物输出：

```text
PredictedObstacle
  states: [T,x,y,yaw,vx,vy]
  size
  covariance: [T,2,2]
  confidence
  source: geometry | learned | fused
```

有效安全膨胀：

```text
margin = footprint_margin
       + k_sigma * sqrt(lambda_max(covariance_xy))
       + abs(relative_speed) * total_latency
       + braking_margin
       + missed_detection_margin
```

检测置信度不能直接当作协方差。

### 5.10 车型专用D-CBF-MPC

统一约束形式：

```text
h(k+1) >= (1-gamma(dt))*h(k) - slack(k)
gamma(dt) = 1 - exp(-alpha*dt)
```

MPC使用实测footprint、几何参考点、控制延迟和制动能力。障碍物可使用圆、capsule、多圆或多边形/距离场表示，不能继续固定为乘用车椭圆。

Top-K求解流程：

1. 首选候选warm start；
2. 在硬截止时间内求解；
3. 检查solver状态、有限值、slack、最小barrier和首控制量；
4. 可行则输出安全轨迹和首控制；
5. 不可行或超时则尝试下一候选；
6. 全部失败进入降速停车。

### 5.11 底盘命令转换

```text
差速/四驱： [v,omega] -> Twist(linear.x, angular.z)
麦轮/全向： [vx,vy,omega] -> Twist(linear.x,linear.y,angular.z)
阿克曼：    [v,delta] -> omega=v/L*tan(delta) -> Twist(linear.x,angular.z)
```

C50C继续接收偏航角速度；阿克曼前轮角由固件内部换算。所有命令先经过饱和、变化率限制和方向约定检查。

### 5.12 车载安全监督

安全监督独立于学习模型和MPC，检查：

- 命令年龄与规划器心跳；
- 点云、odom和IMU新鲜度；
- TF是否可用；
- 当前速度是否超过commissioning/runtime限制；
- 制动距离内是否存在几何障碍；
- 求解器是否超时或返回异常；
- 急停、使能、电池和底盘诊断状态；
- 控制方向是否已完成实车验证。

任一硬故障立即持续发布零速度；不能依赖C50C默认`SecurityLevel=1`保持最后命令的行为。

## 6. 降级与停车链

按以下顺序降级：

```text
首选候选
 -> 下一候选
 -> 保守低速候选
 -> 受控制动轨迹
 -> 持续零速度
 -> 物理急停（人工或硬件链）
```

触发条件与动作：

| 事件 | 动作 |
|---|---|
| UniLION失败 | 切换CenterPoint/几何通路，限制速度 |
| 学习模型全部失败 | 几何停车，不继续盲行 |
| MPC超时 | 丢弃该解，执行上次安全制动或零速度 |
| 点云超时 | 立即停车 |
| odom/IMU超时 | 立即停车 |
| 网络断开 | 车载watchdog持续零速度 |
| TF异常 | 禁止规划并停车 |
| 障碍物过近 | 跳过优化，直接紧急停车 |
| 参数未确认 | 保持`PERCEPTION_ONLY` |

## 7. 离线训练流程

### Stage 0：平台与checkpoint验真

1. 确认底盘`car_mode`、C16子型号、ROS2话题和TF。
2. 录制空场、静态障碍、动态障碍、转弯和停车rosbag。
3. 检查C16点云字段、密度、频率和时间同步。
4. 验证UniLION纯LiDAR checkpoint；失败则切CenterPoint。
5. 记录连续100帧的特征稳定性、显存和P50/P95延迟。

### Stage 1：冻结特征缓存

缓存BEV、目标、输入质量、路线、自车状态及checkpoint/config hash。训练head时不重复运行重型主干。

### Stage 2：候选规划头训练

```text
L_plan = L_best_of_G
       + lambda_cls * L_candidate_classification
       + lambda_div * L_diversity
       + lambda_route * L_route_alignment
       + lambda_dyn * L_dynamics
       + lambda_costmap * L_costmap
```

候选监督来自实车演示、Nav2/规则规划器轨迹或车型专用MPC teacher。

### Stage 3：D-CBF-MPC伪标签

使用R680实际几何、限制、延迟和障碍物数据，对每条候选保存：

```text
feasible, h_min, correction_norm, risk, slack,
objective, solve_time_ms, solve_status, timed_out
```

超时、数值失败和真实不可行必须分开标记。

### Stage 4：安全头与ranker

冻结主干和候选头，先训练安全头与ranker；稳定后才允许小学习率联合微调candidate head。

### Stage 5：可选部分解冻

只有冻结基线稳定且C16域偏移被量化后，才解冻最后一个BEV或temporal block，并与完全冻结方案做消融。

## 8. 实车试验顺序

```text
静态解析与单元测试
 -> rosbag离线回放
 -> 感知-only、底盘禁用
 -> 架空轮控制方向测试
 -> 空旷场地0.1m/s
 -> 静态软障碍
 -> 动态横穿障碍
 -> 点云/odom/网络丢失注入
 -> MPC超时和模型崩溃注入
 -> 逐级提高到标定运行速度
```

每一级必须验收停车距离、命令超时、急停、方向符号、最小barrier、MPC可行率和P95闭环延迟。

## 9. 必须记录的运行数据

- 配置、代码和checkpoint hash；
- C16原始点云统计与丢帧；
- odom/IMU/TF时间戳；
- 所有候选、rank分数和过滤原因；
- MPC请求、结果、slack、barrier和求解时间；
- 最终命令、限幅前后值和底盘反馈；
- watchdog、降级、停车和急停原因；
- CPU/GPU/内存/网络延迟。

## 10. 运动解锁验收门

只有配套配置中的以下门全部为`true`，系统才允许输出非零`/cmd_vel`：

- 唯一底盘型号与`car_mode`已确认；
- footprint和参考点已实测；
- 速度、加速度、制动距离和延迟已标定；
- C16精确型号、话题、字段、频率和外参已确认；
- TF与时间同步验证通过；
- 正负速度和角速度方向验证通过；
- `/cmd_vel`失联停车验证通过；
- 物理急停和软件停车验证通过；
- 感知、MPC和watchdog故障注入验证通过。

